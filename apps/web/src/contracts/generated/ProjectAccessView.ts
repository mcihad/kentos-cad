// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { AccessSource } from "./AccessSource";
import type { ProjectPermission } from "./ProjectPermission";
import type { ProjectRole } from "./ProjectRole";

/**
 * The caller's access to a project: the highest of ownership, a grant and the
 * organisation's policy, and exactly what it allows. Buttons follow it; the
 * server checks every request itself.
 */
export type ProjectAccessView = { role: ProjectRole, via: AccessSource, permissions: Array<ProjectPermission>, };
