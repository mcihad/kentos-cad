/**
 * The contracts version this app was built against: `CONTRACTS_VERSION` in
 * crates/shared/contracts/src/lib.rs (contracts.test.ts keeps the two equal). A
 * server speaking another version is shown as incompatible, not trusted.
 */
export const CONTRACTS_VERSION = 1;
