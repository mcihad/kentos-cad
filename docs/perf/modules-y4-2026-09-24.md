# Ağır modüllerin açılışı: y4 (2026-09-24, e0b9168)

Intel(R) Xeon(R) Processor @ 2.80GHz, 4 iş parçacığı, 16 GB; Chromium 141.0.7390.37; başsız, WebGL2; `vite preview` (yerel). Her modül kendi boş profiliyle, 3 ölçümün ortancası. Süre komut satırında Enter'dan modülün penceresinin belgeye girmesine ve boyandığı kareye (iki animasyon karesi) kadardır. Hedefler: docs/adr/0005 (ilk açılış ≤ 400 ms, ikinci ≤ 150 ms).

| Modül | İlk açılış: pencere | İlk açılış: boyandı (aralık) | İkinci açılış: boyandı (aralık) | İlk açılışta indirilen |
|---|---|---|---|---|
| Stil yöneticisi | 103 ms | 220 ms (215–240) | 107 ms (90–126) | 12.9 KB |
| Model tasarımcısı | 76 ms | 125 ms (122–142) | 36 ms (30–36) | 11.1 KB |
| SVG düzenleyicisi | 114 ms | 163 ms (153–163) | 42 ms (37–66) | 892.2 KB |

Aktarım, sunucunun gönderdiği boyuttur: gzip ile sıkıştırılanlar sıkıştırılmış, öbürleri (WASM dahil) ham.

Konsolda hata yok.

İlk ölçümde ilk açılışın istekleri:

- Stil yöneticisi: `StyleManager-DXa8N58T.js` 7.0 KB (gzip), `thumbs-BZJ5Cdt2.js` 4.7 KB (gzip), `styleFiles-D4VFYQyz.js` 1.2 KB (gzip)
- Model tasarımcısı: `ModelDesigner-BCpIFX_N.js` 11.1 KB (gzip)
- SVG düzenleyicisi: `designerFields-Cnem1Qp3.js` 2.2 KB (gzip), `SvgEditor-Dnw9cgn-.js` 49.1 KB (gzip), `kentos_svg_wasm_bg-CKwpOsM6.wasm` 841.0 KB
